# 说明

面向最终用户的同步工具已迁至仓库根目录 **`socai/`**：

- `socai/一键同步.bat`
- `socai/sync.py`
- `socai/bin/socai.exe`（运行 `scripts/build_socai_pack.ps1` 生成）

网站下载包：`downloads/xhs-favorites-sync-windows.zip`  
图文教程：前端左侧 **「使用教程」**。

本目录 `sync.py` 仅作开发兼容保留，新集成请使用 `socai/sync.py`。
